services:
  app:
    build:
      context: .
      target: production
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      PORT: 3000
      # One-time cutover only: point at an existing Postgres to import its data,
      # then unset. Leave blank for fresh/local installs.
      MIGRATE_FROM_POSTGRES_URL: ${MIGRATE_FROM_POSTGRES_URL:-}
      OIDC_DISCOVERY_URL: ${OIDC_DISCOVERY_URL:-}
      OIDC_ISSUER_URL: ${OIDC_ISSUER_URL:-}
      OIDC_CLIENT_ID: ${OIDC_CLIENT_ID:-}
      OIDC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET:-}
      OIDC_REDIRECT_URI: ${OIDC_REDIRECT_URI:-}
      OIDC_POST_LOGOUT_REDIRECT_URI: ${OIDC_POST_LOGOUT_REDIRECT_URI:-}
      OIDC_SCOPES: ${OIDC_SCOPES:-}
      OIDC_NAME: ${OIDC_NAME:-}
      OIDC_AUTO_CREATE_USER: ${OIDC_AUTO_CREATE_USER:-}
      SESSION_MAX_AGE: ${SESSION_MAX_AGE:-}
      BYPASS_LOGIN: ${BYPASS_LOGIN:-}
    volumes:
      - ./data:/app/backend/data
      - ./uploads:/app/backend/uploads
      - ./fonts:/app/backend/fonts
