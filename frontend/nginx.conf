server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # API and static file proxying to backend
    location /api/ {
        proxy_pass http://backend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /uploads/ {
        proxy_pass http://backend:3000;
    }

    location /fonts/ {
        proxy_pass http://backend:3000;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
